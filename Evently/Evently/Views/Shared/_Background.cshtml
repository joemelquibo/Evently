<style>
    :root {
        --bg-dark: #0d0b21;
        --bg-purple: #3e1f81;
    }

    body, html {
        height: 100%;
        margin: 0;
        overflow: hidden;
        font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
        /* Vibrant Purple Beam Gradient */
        background: radial-gradient(circle at 30% 50%,
                                    var(--bg-purple) 0%,
                                    var(--bg-dark) 45%,
                                    #000 100%);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    }
</style>
